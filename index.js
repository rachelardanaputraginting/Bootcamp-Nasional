// alert('Bootcamp Nasional 2022')

// tipe data
    const bahasa  = 'Javascript'
    const age = 12
    const boolean = true
    let data1

        console.log(typeof bahasa)
        console.log(typeof age)
        console.log(typeof boolean)
        console.log(typeof tes)

    // array
    const data2 = ['string', true, 12, undefined];
    console.log(data2[1])

    // objek
    const person = {
        name : "Rachel Ardana Putra Ginting",
        age : 19,
        city : "Lhokseumawe"
    }

    console.log(person.name)
    console.log(person.age)
    console.log(person.city)
    

    const variabel = 'value'

    const x = 40

    if(x > 60){
        console.log('C')
    }else {
        console.log('D')
    }

    const data3 = ['a', 'b', 'c', 'd', 'e', 'f']
    const length = data3.length

    for(let i = 0; i <= length; i++) {
        console.log(data3[i])
    }

    const print = (data) => {
        console.log(data)
    }

    let events = 'Bootcamp'

    const tra
